const schedule = require('node-schedule');

//schedule.scheduleJob('0 0 * * *', ); // run on every day
//schedule.scheduleJob('0 * * * *', ); // run every hour

//schedule.scheduleJob('0 0 * * *', ); // run on every day
//schedule.scheduleJob('*/10 * * * * *', ); // run on every 10 seconds
////schedule.scheduleJob('0 0 * * *', ); // run on every day 12 am
////schedule.scheduleJob('*/5 * * * *', ); // run on every 5 minutes
//schedule.scheduleJob('*/50 * * * * *', ); // run on every 50 seconds
//schedule.scheduleJob('*/5 * * * *', ); // run on every 5 minutes
